# Bottom Sheet Component Documentation

We are working on building a bottom sheet component, with smooth animations and transitions. as what google maps does. but for react - web application.

your job is ultilize react-epic skills to build this component.
ensure best quality code (Architecture, DX) and performance

we should build this component as standalone module inside src/components/bottom-sheet
